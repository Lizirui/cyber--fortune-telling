# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Web3 NFT project called "赛博算命" (Cyber Fortune). Users mint AI-generated blessing NFTs with different rarity tiers.

## Development Commands

Once project structure is established, common commands will include:

- `bun install` - Install dependencies
- `bun run dev` - Start development server
- `bun run build` - Build for production
- `bun run lint` - Run linter
- `bun test` - Run tests

## Architecture

This is a new project. Once code is added, the architecture will be documented here.
