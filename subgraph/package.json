{
  "name": "cyber-fortune-subgraph",
  "version": "0.0.1",
  "description": "TheGraph subgraph for Cyber Fortune NFT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio cyber-fortune",
    "create-local": "graph create --node http://localhost:8020/ cyber-fortune",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cyber-fortune"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.77.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
