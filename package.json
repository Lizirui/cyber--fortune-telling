{
  "name": "cyber-fortune-nft",
  "version": "1.0.0",
  "private": true,
  "description": "赛博算命 NFT - Cyber Fortune NFT",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
